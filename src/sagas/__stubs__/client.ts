import { ClientItem } from '../../reducers/client'

export const clientDataStub: ClientItem = {
  data: [
    {
      id: '1',
      appName: 'string',
      developerName: 'string',
      developerId: 'string',
      displayImage: 'string',
      displayText: 'string',
      approved: false
    }
  ]
}
