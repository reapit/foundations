import React from 'react'
import { storiesOf } from '@storybook/react'
import SearchComponent from '.'

storiesOf('SearchComponent', module).add('SearchComponent', () => {
  return <SearchComponent />
})
