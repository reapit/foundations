import { ChecklistStatus } from '@/reducers/checklist-detail'

export const checklistStatus: ChecklistStatus = {
  profile: true,
  primaryId: false,
  secondaryId: true,
  addresses: true,
  agentChecks: false,
}
