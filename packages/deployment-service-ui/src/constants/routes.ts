const Routes = {
  HOME: '/',
  LOGIN: '/login',
  PIPELINES: '/pipelines',
  PIPELINES_CREATION: '/pipelines/create',
  PIPELINES_UPDATE: '/pipelines/:pipelineId/update',
  PIPELINES_SHOW: '/pipelines/:pipelineId',
  API_KEYS: '/api-keys',
  RELEASES: '/releases/:projectName',
  RELEASE_PROJECTS: '/releases',
}

export default Routes
