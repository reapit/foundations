const Routes = {
  HOME: '/',
  LOGIN: '/login',
  PIPELINES: '/pipelines',
  PIPELINES_CREATION: '/pipelines/create',
  PIPELINES_SHOW: '/pipelines/:pipelineId',
  API_KEYS: '/api-keys',
  RELEASES: '/releases',
}

export default Routes
