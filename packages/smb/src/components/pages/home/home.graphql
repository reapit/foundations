#import "../../../graphql/fragments/paging.graphql"
#import "../../../graphql/fragments/contact.graphql"

query loadContacts($pageSize: Int!, $pageNumber: Int!) {
  contacts(pageSize: $pageSize, pageNumber: $pageNumber) {
    contacts {
      ...ContactObj
    }
    ...Pagination
  }
}
