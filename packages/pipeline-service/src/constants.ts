export enum TABLE_NAMES {
  PIPELINE = 'Cloud_Pipeline_Service',
  TASK = 'Cloud_Task_Service',
}

export enum Status {
  FAILED = 'failed',
  SUCCESS = 'success',
  CANCELED = 'canceled',
  PENDING = 'pending',
  RUNNING = 'running',
}
