import { FormFieldInfo } from '@reapit/elements-legacy'
type Field = 'terminatedReasonField'

const formFields: Record<Field, FormFieldInfo> = {
  terminatedReasonField: {
    name: 'terminatedReason',
  },
}

export default formFields
