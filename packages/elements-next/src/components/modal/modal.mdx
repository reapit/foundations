---
name: Modal
route: /modal
---

import { Props } from 'docz'
import { Modal } from './modal'
import { DocsWrapper } from '@/utils/docs-wrapper'
import Playground from '@/components/playground'

# Modal
A modal is a window overlaid on either the primary window or another modal window. Contents behind a modal modal are inert meaning that users cannot interact with content behind the modal.

## Properties
<Props of={Modal} />

## Usage
<Playground>
  {() => {
    const [isShowModal, setIsShowModal] = React.useState(false)
    return (
      <DocsWrapper>
        <Modal visible={isShowModal} isCentered={true} title="example" destroyOnClose={true} onClose={() => setIsShowModal(false)}>Heading Main</Modal>
        <button onClick={() => setIsShowModal(true)}>Show Modal</button>
      </DocsWrapper>
    )
  }}
</Playground>
