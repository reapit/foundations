import { apointmentPlannerSvg } from '../book-valuation-widget.styles'

describe('apointmentPlannerSvg', () => {
  it('should run correctly', () => {
    expect(apointmentPlannerSvg).toMatchSnapshot()
  })
})
