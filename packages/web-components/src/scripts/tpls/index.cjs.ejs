<% packages.forEach(function({
  exportName,
  rollUpPackageName
}){ %>
  <% if (exportName) { %>
    const {<%= exportName %>} = require('./<%= rollUpPackageName %>.js')
  <% } %>
<% }); %>

module.exports = {
<% packages.forEach(function({
  exportName,
  rollUpPackageName
}){ %>
  <% if (exportName) { %>
    <%= exportName %>,
  <% } %>
<% }); %>
}

