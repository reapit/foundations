{
  "name": "@reapit/cognito-custom-mail-lambda",
  "version": "1.0.0",
  "description": "Lambda Mailer Function",
  "main": "public/app.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test:ci": "cross-env TZ=UTC jest --ci --colors --coverage --silent --forceExit",
    "test:dev": "cross-env TZ=UTC jest --watch --verbose",
    "build:prod": "rimraf dist && rimraf public && tsc --p tsconfig.json && cp -rf ./src/mailer/templates/ejs ./public/mailer/templates/ejs",
    "release:dev": "cross-env CI=true && yarn build:prod && yarn test:ci && serverless deploy --stage dev",
    "release:prod": "cross-env CI=true && yarn build:prod && yarn test:ci && serverless deploy --stage prod"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/reapit/cognito-custom-mail-lambda.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/reapit/cognito-custom-mail-lambda/issues"
  },
  "homepage": "https://github.com/reapit/cognito-custom-mail-lambda#readme",
  "dependencies": {},
  "devDependencies": {}
}
