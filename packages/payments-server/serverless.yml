service: cloud-payments-portal
plugins:
  - serverless-webpack
  - serverless-offline
  - serverless-deployment-bucket

custom:
  s3CloudFormBucket: cloud-deployment-cloudform-templates-${opt:stage, 'dev'}
  env: ${file(./config.json)}
  webpack:
    webpackConfig: 'webpack.config.js'
    includeModules: true
    packager: 'yarn'
    packagerOptions:
      noFrozenLockFile: false
    excludeFiles: src/**/*.test.ts
    keepOutputDirectory: true

provider:
  name: aws
  runtime: nodejs12.x
  stage: ${opt:stage, 'dev'}
  region: eu-west-2
  deploymentBucket:
    name: ${self:custom.s3CloudFormBucket}
    blockPublicAccess: false
  
  environment:
    NODE_ENV: ${self:custom.env.NODE_ENV}
    APP_ENV: ${self:custom.env.APP_ENV}
    STRIPE_SECRET_KEY: ${self:custom.env.STRIPE_SECRET_KEY}
    STRIPE_PUBLISHABLE_KEY: ${self:custom.env.STRIPE_PUBLISHABLE_KEY}
    STRIPE_CLIENT_ID: ${self:custom.env.STRIPE_CLIENT_ID}
    SENTRY_DSN: ${self:custom.env.SENTRY_DSN}
    CLIENT_URL: ${self:custom.env.CLIENT_URL}

package:
  individually: true
  include:
    - dist/**
  exclude:
    - package.json
    - node_modules/**
    - error.log
    - info.log
    - jest.config.js
    - serverless.yml
    - src/**
    - tsconfig.json
    - yml-helpers.js
    - public/**

functions:
  payments-portalServer:
    handler: src/index.app
    events:
      - http: ANY /
      - http: 'ANY {proxy+}'

