import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { Label } from './index'

<Meta title="V3/Forms/Label" component={Label} />

# Label

Underlying tag: `<label>`

<Canvas>
  <Story name="Default Usage">
    <Label>I'm a label for a form input</Label>
  </Story>
</Canvas>

# Other attributes

All other standard HTML attributes for `<label>` are supported and are passed through to the React component, e.g. `for`
