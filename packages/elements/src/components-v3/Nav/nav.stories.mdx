import { Story, Canvas } from '@storybook/addon-docs/blocks'
import { cx } from 'linaria'
import { Nav } from './index'

<Meta title="V3/Nav" />

# Modal

# React usage

## Complete example with `useNav` hook

<Canvas>
  <Story name="useNav example">
    {() => {
      return (
        <>
          <Nav />
        </>
      )
    }}
  </Story>
</Canvas>
