import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { InputError } from './index'
import { Input } from './../input'
import { InputGroup } from './../input-group'
import { RenderHtmlMarkup } from '../../storybook/render-html-markup'

<Meta title="InputError" component={InputError} />

# Input Error

A simple component used to display an error message under an input component

## Example

<Canvas>
  <Story name="Error Message">
    <InputGroup
      hasError
      name="email address"
      type="email"
      placeholder="Not an email"
      errorMessage="Email address provided is not a valid email address"
    ></InputGroup>
  </Story>
</Canvas>

<RenderHtmlMarkup component="InputError" story="Error Message" />
