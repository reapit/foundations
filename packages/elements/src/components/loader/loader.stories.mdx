import { useState } from 'react'
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { Loader } from './index'
import { RenderHtmlMarkup } from '../../storybook/render-html-markup'

<Meta title="Loader" component={Loader} />

# Loader

A simple loader component to give a visual indicator of progress. It ships with an optional label prop as per below and can be shown inline or centered.

<ArgsTable of={Loader} />

## Inline

<Canvas>
  <Story name="Inline">
    <Loader />
  </Story>
</Canvas>

<RenderHtmlMarkup component="Loader" story="Inline" />

## With a label

<Canvas>
  <Story name="With a label">
    <Loader label="Loading" />
  </Story>
</Canvas>

<RenderHtmlMarkup component="Loader" story="With a label" />

## Full page

The full page loader overlays any content and centers on the page - suitable for use when loading entire pages of content.

<Canvas>
  <Story name="Full page loader">
    <p>This loader is fixed to the center of the screen</p>
    <Loader fullPage label="Loading" />
  </Story>
</Canvas>

<RenderHtmlMarkup component="Loader" story="Full page loader" />

# Other attributes

`Loader` takes no custom props but all other standard HTML attributes for `<div>` are supported and are passed through to the React component, e.g. `className`
