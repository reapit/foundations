import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { Button, ButtonGroup, FloatingButton } from './index'
import { RenderHtmlMarkup } from '../../storybook/render-html-markup'
import { action } from '@storybook/addon-actions'

<Meta title="Button" component={Button} />

# Button

Styled button component with multiple flavours depending on use case.

Props available for the React version of the component;

<ArgsTable of={Button} />

## Default Usage

A button with no addtional props / classes applied.

<Canvas>
  <Story name="Default Usage">
    <Button>Text within button</Button>
  </Story>
</Canvas>

<RenderHtmlMarkup component="Button" story="Default Usage" />

## With Intent

Different intents are supported, to enable buttons to be used in different contexts. Typcially you will always choose an intent for your button. The intent prop maps directly to the intent helper classes for non React users.

Whereas most components support the full range of intents, `Buttons` only support `primary`, `danger` and `neutral`, the latter being the default.

<Canvas>
  <Story
    name="Intent"
    args={{
      intent: 'primary',
      children: 'Button Text',
    }}
  />
</Canvas>

<RenderHtmlMarkup component="Button" story={['Intent']} label="HTML of intent" />

## Is Loading

Add a loading spinner to indicate progress of a background activity after the button was clicked.

<Canvas>
  <Story
    name="Loading"
    args={{
      loading: true,
      children: 'Button Text',
    }}
  />
</Canvas>

<RenderHtmlMarkup component="Button" story={['Loading']} label="HTML of the above" />

## Is Disabled

Indicate that a button is disabled for whatever reason.

<Canvas>
  <Story
    name="Disabled"
    args={{
      disabled: true,
      children: 'Button Text',
    }}
  />
</Canvas>

<RenderHtmlMarkup component="Button" story={['Disabled']} />

## Other attributes

All other standard HTML attributes for `<button>` are supported and are passed through to the React component, e.g. type and onClick

<Canvas>
  <Story
    name="Standard attributes"
    args={{
      onClick: action('Button was clicked'),
      type: 'submit',
      children: 'Button Text',
    }}
  />
</Canvas>

<RenderHtmlMarkup component="Button" story={['Standard attributes']} />

## Button Group

An element to collectively group several buttons

<Canvas>
  <Story name="Button Group">
    <ButtonGroup>
      <Button intent="primary">Primary</Button>
      <Button intent="neutral">Neutral</Button>
      <Button intent="danger">Danger</Button>
    </ButtonGroup>
  </Story>
</Canvas>

<RenderHtmlMarkup component="Button" story="Button Group" />

## Floating Button

A circular floating icon button - supports any icon from the icon list.

Props available for the React version of the component are the same as the main button.

<Canvas>
  <Story name="Floating Button" component={FloatingButton}>
    <FloatingButton intent="primary" icon="addSystem" />
    <FloatingButton intent="neutral" icon="addSystem" />
    <FloatingButton intent="danger" icon="addSystem" />
  </Story>
</Canvas>

<RenderHtmlMarkup component="Button" story="Floating Button" />
