import { Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { Toggle, ToggleRadio } from './index'
import { RenderHtmlMarkup } from '../../storybook/render-html-markup'
import { ElToggleItem } from './__styles__'

<Meta title="Toggle" component={Toggle} />

# Toggle

## Basic Usage

The toggle component is mask for a checkbox and for React users can be used as a drop in for this input type. You therefore manage state using any Form library or by referencing the `onChange` input event.

For non React users, you can just apply the classes in the rendered markup to your input and label elements.

Typically you will use this component when you want the user to select binary options like off/on or active/inactive.

Props available for the React version of the component are just those of a standard React checkbox with some variant props as per below.

<ArgsTable of={Toggle} />

<Canvas>
  <Story name="Basic Usage">
    <Toggle id="my-cool-toggle">
      <ElToggleItem>On</ElToggleItem>
      <ElToggleItem>Off</ElToggleItem>
    </Toggle>
  </Story>
</Canvas>

<RenderHtmlMarkup component="Toggle" story="Basic Usage" />

## Toggle - Grey Bg

Where rendering a toggle on a white background, you should apply the hasGreyBg prop to give better contrast. The white background standard toggle is intended for use on a grey background.

<Canvas>
  <Story name="Toggle - Grey Bg">
    <Toggle id="my-cool-toggle-variants-grey" hasGreyBg>
      <ElToggleItem>On</ElToggleItem>
      <ElToggleItem>Off</ElToggleItem>
    </Toggle>
  </Story>
</Canvas>

<RenderHtmlMarkup component="Toggle" story="Toggle - Grey Bg" />

## Multi Toggle - ToggleRadio

The `ToggleRadio` component uses very similar markup as the binary toggle but expands it to allow radio options. As before, you can manage state using any Form library or by referencing the `onChange` input event.

For react users, the component does not render children, rather it accepts a list of options in the following interface and maps through them on render.

As per the previous example, for non React users, you can just apply the classes in the rendered markup to your input and label elements.

Typically you will use this component when you want the user to select from a number of options, possibly filters or similar select options where a dropdown is not appropriate.

Props available for the React version of the component;

<ArgsTable of={ToggleRadio} />

Where `ToggleRadioOption` is defined as;

```tsx
export interface ToggleRadioOption {
  id: string
  value: string
  text: string
  isChecked: boolean
}
```

<Canvas>
  <Story name="Toggle Radio - Default">
    <ToggleRadio
      name="my-cool-toggle-radio"
      options={[
        {
          id: 'option-1',
          value: 'option-1',
          text: 'Option 1',
          isChecked: true,
        },
        {
          id: 'option-2',
          value: 'option-2',
          text: 'Option 2',
          isChecked: false,
        },
        {
          id: 'option-3',
          value: 'option-3',
          text: 'Option 3',
          isChecked: false,
        },
      ]}
    />
  </Story>
</Canvas>

<RenderHtmlMarkup component="Toggle" story="Toggle Radio - Default" />

## Toggle RadioSelectProps - Grey Bg

Where rendering a toggle on a white background, you should apply the hasGreyBg prop to give better contrast. The white background standard toggle is intended for use on a grey background.

<Canvas>
  <Story name="Toggle RadioSelectProps - Grey Bg">
    <ToggleRadio
      name="my-cool-toggle-radio-grey"
      hasGreyBg
      options={[
        {
          id: 'option-1-fw-grey',
          value: 'option-1-fw-grey',
          text: 'Option 1',
          isChecked: true,
        },
        {
          id: 'option-2-fw-grey',
          value: 'option-2-fw-grey',
          text: 'Option 2',
          isChecked: false,
        },
        {
          id: 'option-3-fw-grey',
          value: 'option-3-fw-grey',
          text: 'Option 3',
          isChecked: false,
        },
      ]}
    />
  </Story>
</Canvas>

<RenderHtmlMarkup component="Toggle" story="Toggle RadioSelectProps - Grey Bg" />

## Toggle Disabled


<Canvas>
  <Story name="Toggle Radio - Disabled">
    <ToggleRadio
      name="my-cool-toggle-radio"
      disabled={true}
      options={[
        {
          id: 'option-1',
          value: 'option-1',
          text: 'Option 1',
          isChecked: true,
        },
        {
          id: 'option-2',
          value: 'option-2',
          text: 'Option 2',
          isChecked: false,
        },
        {
          id: 'option-3',
          value: 'option-3',
          text: 'Option 3',
          isChecked: false,
        },
      ]}
    />
  </Story>
</Canvas>

<RenderHtmlMarkup component="Toggle" story="Toggle Radio - Disabled" />
